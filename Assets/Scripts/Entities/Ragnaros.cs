using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Ragnaros : CharacterCard
{

    // Start is called before the first frame update
    void Start()
    {
        base.PassData();
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
